const users = [
    {
        name: "Mehmet",
        age: 30,
    },
    {
        name: "Mehmet",
        age: 29,
    },
    {
        name: "Murat",
        age: 35,
    },    
    ];

/*
    push
    map
    find
    filter
    some
    every
    includes
*/

//push
// users.push("Ayşe");
// console.log(users);

//map
// users.map((item) => {
//     console.log(item);
// });

//find
// const result = users.find((item) => item.name === "Mehmet");
// console.log(result);

//filter
// const filtered = users.filter((item) => item.name === "Mehmet" && item.age === 29);
// console.log(filtered);

// some
// const some = users.some((item) => item.age === 29);
// console.log(some);

// every
// const every = users.every((item) => item.age > 12);
// console.log(every);

// includes
// const meyveler = ["elma","armut","muz"];
// const isIncluded = meyveler.includes("muz");
// console.log(isIncluded);